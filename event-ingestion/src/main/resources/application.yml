spring:
  application:
    name: event-ingestion
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  datasource:
    url: jdbc:sqlite:camelshop.db
  jpa:
    show-sql: true
camelshop:
    rabbitmq:
        exchange: eventingestion.exchange
        queue: eventingestion.queue
        routingKey: camelshop.#